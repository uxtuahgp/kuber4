apiVersion: v1
kind: Service
metadata:
  name: dep-service
spec:
  selector:
    app: dep-app
  ports:
    - name: nginx-port
      protocol: TCP
      port: 10080
      targetPort: 80
    - name: multitool-port
      protocol: TCP
      port: 10081
      targetPort: 8080
